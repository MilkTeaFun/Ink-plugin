{
  "schemaVersion": 1,
  "kind": "source",
  "pluginKey": "python-hello-source",
  "name": "Python Hello Source",
  "version": "1.0.0",
  "description": "Returns a configurable greeting for local plugin upload testing.",
  "runtime": {
    "type": "python"
  },
  "entrypoints": {
    "validate": {
      "command": ["python3", "validate.py"]
    },
    "fetch": {
      "command": ["python3", "fetch.py"]
    }
  },
  "workspaceConfigSchema": [
    {
      "key": "sourceName",
      "label": "Source Name",
      "type": "text",
      "required": true,
      "description": "Displayed in the generated print title.",
      "defaultValue": "Python Hello Source"
    },
    {
      "key": "apiToken",
      "label": "API Token",
      "type": "secret",
      "required": false,
      "description": "Optional token kept only in encrypted binding storage."
    }
  ],
  "scheduleConfigSchema": [
    {
      "key": "message",
      "label": "Message",
      "type": "textarea",
      "required": true,
      "description": "Content printed by the plugin.",
      "defaultValue": "Hello from the Python fixture plugin."
    },
    {
      "key": "uppercase",
      "label": "Uppercase Output",
      "type": "checkbox",
      "required": false,
      "defaultValue": false
    }
  ]
}
